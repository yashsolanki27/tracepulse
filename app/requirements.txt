# CPU-only torch is installed in the Dockerfile via
# --index-url https://download.pytorch.org/whl/cpu BEFORE this file is
# processed, so torch resolves to the CPU wheel (no nvidia_*/cuda deps).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.52
psycopg2-binary>=2.9.12
pgvector>=0.5.0
pydantic>=2.13.5
alembic>=1.19.1
openai>=1.0.0
torch>=2.0.0
sentence-transformers>=3.0.0
